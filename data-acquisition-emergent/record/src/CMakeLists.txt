# Source files
target_sources("${PROJECT_NAME}" PRIVATE
    "${CMAKE_CURRENT_SOURCE_DIR}/recordtask.cpp"
)

# Header files
target_sources("${PROJECT_NAME}" PRIVATE
    "${CMAKE_CURRENT_SOURCE_DIR}/recordtask.h"
)
